CREATE TABLE connectwithgamers.pricings_enum (
    pricings_enum_sys_key_id int NOT NULL AUTO_INCREMENT,
    name varchar(50) NOT NULL,
    PRIMARY KEY (pricings_enum_id)
);

INSERT INTO connectwithgamers.pricings_enum VALUES ("Free");

INSERT INTO connectwithgamers.pricings_enum VALUES ("Free with Xbox Game Pass");

INSERT INTO connectwithgamers.pricings_enum VALUES ("Discounted with Xbox Game Pass");

INSERT INTO connectwithgamers.pricings_enum VALUES ("Discounted with Xbox Live Gold");

INSERT INTO connectwithgamers.pricings_enum VALUES ("Main game");

INSERT INTO connectwithgamers.pricings_enum VALUES ("DLC/Bundles");

INSERT INTO connectwithgamers.pricings_enum VALUES ("Coming Soon");

INSERT INTO connectwithgamers.pricings_enum VALUES ("Preorder");

INSERT INTO connectwithgamers.pricings_enum VALUES ("In-App Purchase");

CREATE TABLE connectwithgamers.pricings VALUES (
    pricings_sys_key_id int NOT NULL AUTO_INCREMENT,
    igdb_external_enum_id int NOT NULL,
    igdb_pricings_enum_id int NOT NULL
    igdb_id int NOT NULL,
    price <PREVTYPE>,
    discount_percent int,
    url <PREVTYPE>,
    expires <PREVTYPE>,
    PRIMARY KEY (pricings_id),
    FORIEGN KEY (igdb_id) REFERENCES games(igdb_id),
    FORIEGN KEY (igdb_external_enum_id) REFERENCES igdb_external_enum(igdb_external_enum_id),
    FORIEGN KEY (pricings_enum_sys_key_id) REFERENCES pricings_enum(pricings_enum_sys_key_id),
    UNIQUE KEY(igdb_external_enum_id,igdb_pricings_id,igdb_id)
);

CREATE TABLE connectwithgamers.accounts_role_enum {
    accounts_role_enum_sys_key_id int NOT NULL AUTO_INCREMENT,
    name varchar(30) NOT NULL,
    PRIMARY KEY (accounts_role_enum_sys_key_id)
}

INSERT INTO connectwithgamers.accounts_role_enum VALUES ("Basic");

INSERT INTO connectwithgamers.accounts_role_enum VALUES ("Game Developer");

INSERT INTO connectwithgamers.accounts_role_enum VALUES ("Editor");

INSERT INTO connectwithgamers.accounts_role_enum VALUES ("Moderator");

INSERT INTO connectwithgamers.accounts_role_enum VALUES ("Admin");

CREATE TABLE connectwithgamers.accounts_roles {
    accounts_roles_sys_key_id int NOT NULL AUTO_INCREMENT,
    accounts_role_enum_sys_key_id int NOT NULL,
    accounts_sys_key_id int NOT NULL,
    FORIEGN KEY (accounts_role_enum_sys_key_id) REFERENCES accounts_role_enum(accounts_role_enum_sys_key_id),
    FORIEGN KEY (accounts_sys_key_id) REFERENCES accounts(accounts_sys_key_id),
    PRIMARY KEY (accounts_roles_sys_key_id)
    UNIQUE KEY (accounts_role_enum_sys_key_id,accounts_sys_key_id)
}

CREATE TABLE connectwithgamers.accounts {
    accounts_sys_key_id int NOT NULL AUTO_INCREMENT,
    username varchar(16) NOT NULL,
    email varchar(100) NOT NULL,
    password_hash varchar(60) NOT NULL,
    salt varchar(29) NOT NULL,
    log_date datetime NOT NULL,
    discord varchar(100),
    twitch varchar(100),
    steam varchar(100),
    email_verification_code varchar(50),
    recovery_verification_code varchar(32),
    PRIMARY KEY (accounts_sys_key_id)
}

- auto increment previous tables, rename sys_key_id, redo primary keys
